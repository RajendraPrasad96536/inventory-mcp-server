services:
  - type: web
    name: medicine-mcp-server
    runtime: python
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
    buildCommand: "pip install --upgrade pip && pip install . --no-cache-dir"
    startCommand: "python server.py"